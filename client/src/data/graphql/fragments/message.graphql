fragment MessageModel on Message {
    id,
    text,
    createdAt,
    updatedAt
    sender {
        ...UserModel
    },
    chat {
        id,
        name,
        avatar {
            id,
            url
        }
    }
    replyTo {
        id,
        text,
        createdAt,
        updatedAt
        sender {
            name,
            avatar {
                url
            },
        },
    },
    attachments {
        id,
        originalName,
        createdAt,
        url,
        type
    }
}
